version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: rustberg
      POSTGRES_PASSWORD: rustberg
      POSTGRES_DB: rustberg
    ports:
      - "5432:5432"
    volumes:
      - rustberg-pg-data:/var/lib/postgresql/data

volumes:
  rustberg-pg-data:
